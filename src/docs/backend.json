
{
  "entities": {
    "AppSettings": {
      "title": "Application Settings",
      "description": "Global settings for the entire application. Can be used for feature flags, global messages, or other dynamic configurations.",
      "type": "object",
      "properties": {
        "fontSizeMultiplier": {
          "type": "number",
          "description": "A multiplier to adjust the base font size for all users."
        },
        "taskCategories": {
          "type": "array",
          "description": "An array of strings representing the available task categories.",
          "items": {
            "type": "string"
          }
        },
        "pastelBackgroundEnabled": {
          "type": "boolean",
          "description": "Whether the pastel background is enabled for all users."
        },
        "pastelBackgroundColor": {
          "type": "string",
          "description": "The HSL value for the pastel background color (e.g., '240 60% 95%')."
        },
        "adminEmails": {
            "type": "array",
            "description": "A list of email addresses for users who have admin privileges.",
            "items": {
                "type": "string",
                "format": "email"
            }
        }
      }
    },
    "UserProfile": {
      "title": "User Profile",
      "description": "Represents a user's complete profile and application-specific data stored in Firestore.",
      "type": "object",
      "properties": {
        "uid": {
          "type": "string",
          "description": "The user's unique ID from Firebase Authentication, acting as the document key."
        },
        "displayName": {
          "type": "string",
          "description": "The user's display name, synced from their auth provider."
        },
        "email": {
          "type": "string",
          "format": "email",
          "description": "The user's email address, synced from their auth provider."
        },
        "photoURL": {
          "type": "string",
          "format": "uri",
          "description": "URL of the user's profile picture, synced from their auth provider."
        },
        "provider": {
          "type": "string",
          "description": "The authentication provider used for the last sign-in (e.g., 'google.com')."
        },
        "points": {
          "type": "number",
          "description": "Total points earned by the user for completing tasks and other activities."
        },
        "credits": {
            "type": "number",
            "description": "Currency that can be purchased or earned, used for unlocking special features."
        },
        "level": {
          "type": "number",
          "description": "The user's current level, which may unlock new features or rewards."
        },
        "totalEarned": {
            "type": "number",
            "description": "A lifetime counter of all points ever earned, used for level progression."
        },
        "referralCode": {
          "type": "string",
          "description": "A unique, auto-generated code for referring other users."
        },
        "referredBy": {
          "type": ["string", "null"],
          "description": "The UID of the user who referred this user, if applicable."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "The timestamp of when the user document was first created (first login)."
        },
        "lastLogin": {
          "type": "string",
          "format": "date-time",
          "description": "The timestamp of the user's last login, updated on every sign-in."
        },
        "totalLogins": {
            "type": "number",
            "description": "A counter for the total number of times the user has logged in."
        },
        "settings": {
            "type": "object",
            "description": "User-configurable settings for the application.",
            "properties": {
                "notificationsEnabled": {
                    "type": "boolean",
                    "description": "Whether the user has enabled push notifications."
                },
                "darkMode": {
                    "type": "boolean",
                    "description": "Whether the user has enabled dark mode."
                },
                "theme": {
                  "type": "string",
                  "description": "The user's preferred theme for the application.",
                  "enum": ["default", "reactbits", "midnight", "sunrise", "forest"]
                }
            },
            "required": ["notificationsEnabled", "darkMode", "theme"]
        },
        "interests": {
            "type": "array",
            "description": "An array of strings representing the user's selected interests or categories.",
            "items": {
                "type": "string"
            }
        }
      },
      "required": [
        "uid",
        "displayName",
        "email",
        "photoURL",
        "provider",
        "points",
        "credits",
        "level",
        "totalEarned",
        "referralCode",
        "createdAt",
        "lastLogin",
        "totalLogins",
        "settings",
        "interests"
      ]
    },
    "Task": {
      "title": "Task",
      "description": "A global task definition available to all users.",
      "type": "object",
      "properties": {
        "title": { "type": "string" },
        "description": { "type": "string" },
        "points": { "type": "number" },
        "type": { "type": "string", "enum": ["video", "read"] },
        "category": { "type": "string" },
        "link": { "type": "string", "format": "uri" },
        "content": { "type": "string" }
      },
      "required": ["title", "description", "points", "type", "category", "content"]
    },
    "UserTask": {
      "title": "User Task",
      "description": "Represents the state of a task for a specific user.",
      "type": "object",
      "properties": {
        "status": { "type": "string", "enum": ["available", "in-progress", "completed"] },
        "completedAt": { "type": ["string", "null"], "format": "date-time" },
        "screenshotURL": { "type": "string" }
      },
      "required": ["status"]
    },
    "Feedback": {
      "title": "User Feedback",
      "description": "Represents a feedback submission from a user.",
      "type": "object",
      "properties": {
        "userId": {
          "type": "string",
          "description": "The UID of the user submitting the feedback."
        },
        "userDisplayName": {
          "type": "string",
          "description": "The display name of the user at the time of submission."
        },
        "content": {
          "type": "string",
          "description": "The rich text content of the feedback."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "The timestamp of when the feedback was submitted."
        }
      },
      "required": ["userId", "userDisplayName", "content", "createdAt"]
    }
  },
  "auth": {
    "providers": [
      "google.com"
    ]
  },
  "firestore": {
     "structure": [
      {
        "path": "/users/{userId}",
        "definition": {
          "schema": { "$ref": "#/entities/UserProfile" },
          "description": "Stores all public and private data for a user.",
          "subcollections": [
            {
              "path": "/tasks/{taskId}",
              "definition": {
                "schema": { "$ref": "#/entities/UserTask" },
                "description": "Tracks the status of individual tasks for the user."
              }
            }
          ]
        }
      },
      {
        "path": "/tasks/{taskId}",
        "definition": {
          "schema": { "$ref": "#/entities/Task" },
          "description": "A global list of all available tasks in the application."
        }
      },
      {
        "path": "/app-settings/{settingId}",
        "definition": {
          "schema": { "$ref": "#/entities/AppSettings" },
          "description": "Stores global application settings. Use a single document with a fixed ID like 'global' or 'taskCategories'."
        }
      },
      {
        "path": "/feedback/{feedbackId}",
        "definition": {
          "schema": { "$ref": "#/entities/Feedback" },
          "description": "Stores user-submitted feedback and suggestions."
        }
      }
    ]
  }
}
